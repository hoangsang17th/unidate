import 'package:flutter/material.dart';

class MyPicturesView extends StatelessWidget {
  const MyPicturesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}